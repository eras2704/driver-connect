import Link from "next/link";
import { PortalNav } from "./portal-nav";
import { LogoutButton } from "../admin/logout-button";
export function PortalShell({ children, name, slug, published = false, demo = false }: { children: React.ReactNode; name: string; slug: string; published?: boolean; demo?: boolean }) {
  return <div className="portal-shell"><a className="skip-link" href="#contenido">Saltar al contenido</a><aside className="portal-sidebar"><Link className="portal-brand" href={demo ? "/" : "/panel"}>dc<span>.</span><small>DRIVER CONNECT</small></Link><span className="portal-section-label">TU ESPACIO</span><PortalNav demo={demo} /><div className="sidebar-profile"><span className="portal-avatar">{name.split(/\s+/).slice(0, 2).map(n => n[0]).join("")}</span><div><strong>{name}</strong><span>{demo ? "Perfil de demostración" : published ? "Perfil publicado" : "Perfil en borrador"}</span></div></div></aside><div className="portal-content"><header className="portal-topbar"><span>Profesionales en movimiento<span className="topbar-dot">·</span><strong>Panamá</strong></span><div>{published && <Link href={`/conductor/${slug}`} target="_blank" rel="noopener noreferrer">Ver mi perfil ↗</Link>}{demo ? <Link className="text-link" href="/login-conductor">Entrar a mi cuenta →</Link> : <LogoutButton audience="driver" />}</div></header><main className="portal-main" id="contenido">{children}</main><footer className="portal-footer">Driver Connect <span>Una conexión. Más posibilidades.</span></footer></div></div>;
}
